<nav
  class="{{if @visible 'block' 'hidden'}}
    md:block bg-green-800 text-white
    {{if @visible 'absolute right-6 z-50 md:relative md:right-0 md:z-auto'}}
    w-1/2 md:w-full"
>
  <ul class="flex flex-col md:flex-row md:w-full">
    <li><LinkTo
        class="px-2 pt-2 hover:bg-green-100 hover:text-green-900 font-title"
        {{on "click" @action}}
        @route="about"
      >About</LinkTo></li>
    <li><LinkTo
        class="px-2 pt-2 hover:bg-green-100 hover:text-green-900 font-title"
        {{on "click" @action}}
        @route="essay"
      >Essay</LinkTo></li>
    <li><LinkTo
        class="px-2 pt-2 hover:bg-green-100 hover:text-green-900 font-title"
        {{on "click" @action}}
        @route="documents"
      >Documents</LinkTo></li>
    <li><LinkTo
        class="px-2 pt-2 hover:bg-green-100 hover:text-green-900 font-title"
        {{on "click" @action}}
        @route="credits"
      >Credits</LinkTo></li>
    <li><LinkTo
        class="px-2 pt-2 hover:bg-green-100 hover:text-green-900 font-title"
        {{on "click" @action}}
        @route="bibliography"
      >Bibliography</LinkTo></li>
  </ul>
</nav>